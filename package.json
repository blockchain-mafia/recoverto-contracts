{
  "name": "recoverto-contracts",
  "version": "0.0.1",
  "description": "Ethereum contracts for recover.to",
  "scripts": {
    "build": "npm run build:contracts && npm run build:aws-lambda",
    "build:contracts": "truffle compile",
    "build:aws-lambda": "make -C aws-lambda build",
    "test": "truffle test"
  },
  "author": "Miao ZhiCheng <miao@decentral.ee>",
  "license": "MIT",
  "devDependencies": {
    "@kleros/kleros-interaction": "file:../../kleros/kleros-interaction",
    "openzeppelin-test-helpers": "^0.1.2",
    "truffle": "^5.0.5",
    "truffle-contract": "^4.0.6",
    "web3-hdwallet-provider": "^1.0.4-fix2",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.2.3"
  }
}
