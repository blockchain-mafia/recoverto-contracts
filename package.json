{
  "name": "recoverto-contracts",
  "version": "0.0.1",
  "description": "Ethereum contracts for recover.to - Lost & Found service",
  "scripts": {
    "clean": "rm -rf build node_modules",
    "build": "npm run build:contracts && npm run build:aws-lambda",
    "build:contracts": "truffle compile",
    "build:aws-lambda": "make -C aws-lambda build",
    "test": "truffle compile && truffle test"
  },
  "author": "Miao ZhiCheng <miao@decentral.ee>, Wagner Nicolas <contact@wagner-nicolas.com>",
  "license": "MIT",
  "devDependencies": {
    "@kleros/kleros-interaction": "^0.9.0",
    "openzeppelin-test-helpers": "^0.1.2",
    "truffle": "^5.7.8",
    "truffle-contract": "^4.0.6",
    "truffle-flattener": "^1.3.0",
    "web3-hdwallet-provider": "^1.0.4-fix2",
    "web3-provider-engine": "git+https://github.com/cgewecke/provider-engine.git#web3-one",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.2.3"
  }
}
